# Water Favorability Explorer – Python dependencies
# Install with: pip install -r python/requirements.txt

# Core raster/geo
rasterio>=1.3.0
numpy>=1.24.0
pyproj>=3.5.0
scipy>=1.10.0

# DEM flow routing (choose one or both; pysheds is easiest to install)
# pysheds is a pure-Python D8/D∞ flow routing library
pysheds>=0.4.0

# WhiteboxTools Python wrapper (optional, heavier but more complete)
# whitebox>=2.3.0

# For Copernicus DEM download (Phase 2 / real DEM implementation)
# requests>=2.31.0
# boto3>=1.28.0   # if using AWS S3 direct tile download
