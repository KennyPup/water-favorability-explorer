# Water Favorability Explorer – Python dependencies
# Install with: pip install -r python/requirements.txt

# Core raster/geo
rasterio>=1.3.0
numpy>=1.24.0
pyproj>=3.5.0
scipy>=1.10.0

# Image export for HF map preview overlay
Pillow>=10.0.0

# DEM flow routing (pysheds is pure-Python D8, easiest to install)
pysheds>=0.4.0

# WhiteboxTools Python wrapper (optional, heavier)
# whitebox>=2.3.0
